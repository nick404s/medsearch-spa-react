const SearchParameters = {
  SEARCH_BY: "searchBy",
  SEARCH_TERM: "searchTerm",
  INGREDIENT: "ingredient",
  CONDITION: "condition",
};

const BASE_URL = "https://api.fda.gov/drug";

export { SearchParameters, BASE_URL };
